/**
 * @module StorageLocations models/storageLocations
 * @description This module exports the storage locations.
 *
 */
export const StorageLocations = Object.freeze({
  SessionStorage: "sessionStorage",
  LocalStorage: "localStorage",
});
